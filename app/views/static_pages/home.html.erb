<% provide(:page_name, "Home") %>
<% @posts.each do |post| %>
  <div class="post">
    <h1><%= post.title %></h1>
    <%= post.body %>
    <p class="small">By <%= link_to post.author.username, post.author %></p>
  </div>
<% end %>
